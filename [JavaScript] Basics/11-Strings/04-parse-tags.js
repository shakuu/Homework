//jshint esversion : 6

let openUp = '<upcase>';
let closeUp = '</upcase>';
let openLow = '<lowcase>';
let closeLow = '</lowcase>';
let openMix = '<mixcase>';
let closeMix = '</mixcase>';

function ParseTags(text) {
  var parse = String(text);
}